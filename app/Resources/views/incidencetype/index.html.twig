{% extends 'base.html.twig' %}

{% block body %}
    <h1>Incidencetypes list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for incidenceType in incidenceTypes %}
            <tr>
                <td><a href="{{ path('incidencetype_show', { 'id': incidenceType.id }) }}">{{ incidenceType.id }}</a></td>
                <td>{{ incidenceType.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('incidencetype_show', { 'id': incidenceType.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('incidencetype_edit', { 'id': incidenceType.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('incidencetype_new') }}">Create a new incidenceType</a>
        </li>
    </ul>
{% endblock %}
