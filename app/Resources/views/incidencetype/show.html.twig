{% extends 'base.html.twig' %}

{% block body %}
    <h1>Incidencetype</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ incidenceType.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ incidenceType.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('incidencetype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('incidencetype_edit', { 'id': incidenceType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
